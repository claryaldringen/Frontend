{layout '@layout.latte'}

{block content}
	{include 'menu.latte', 'menu' => $items, 'url' => ''}
	{if empty($isSinglePage)}
		{include 'containers.latte'}
	{else}
		{include 'single.latte'}
	{/if}
{/block}
